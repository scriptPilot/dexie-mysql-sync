{
  "name": "dexie-mysql-sync",
  "version": "5.1.0",
  "description": "",
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "test": "cd demo-app && npm install && npm run lint && npm run build",
    "preversion": "npm install && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scriptpilot/dexie-mysql-sync.git"
  },
  "author": "scriptPilot <mail@scriptPilot.de>",
  "license": "MIT",
  "dependencies": {
    "js-crud-api": "^0.4.0",
    "uuid": "^9.0.1"
  }
}
